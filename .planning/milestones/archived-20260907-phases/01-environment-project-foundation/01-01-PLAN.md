---
phase: 01-environment-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .gitignore
  - README.md
  - src/__init__.py
  - src/data/__init__.py
  - src/preprocessing/__init__.py
  - src/features/__init__.py
  - src/models/__init__.py
  - src/utils/__init__.py
  - src/utils/setup_env.py
autonomous: true
requirements:
  - ENV-01
  - ENV-02
must_haves:
  truths:
    - "D-01: Standard Python venv and pip configuration with pinned requirements.txt"
    - "D-02: NLTK exclusively used for NLP preprocessing without heavy spaCy dependencies"
    - "D-03: src/utils/setup_env.py validates dependencies and bootstraps NLTK resources automatically"
    - "D-04: Structured modular package layout established under src/ with data, models, notebooks, and app directories"
  artifacts:
    - path: "requirements.txt"
      provides: "Python dependency specification"
      contains: "scikit-learn"
    - path: "src/utils/setup_env.py"
      provides: "Automated environment verification and NLTK corpus bootstrap utility"
      min_lines: 40
    - path: "README.md"
      provides: "Project setup documentation and execution instructions"
      contains: "AI-Based Spam Email Detection"
  key_links:
    - from: "src/utils/setup_env.py"
      to: "requirements.txt"
      via: "verifies required libraries and downloads NLTK corpora (punkt, stopwords, wordnet)"
      pattern: "nltk\\.download"
---

<objective>
Establish the foundational project environment, directory layout, dependency specifications, and automated environment verification script for the AI-Based Spam Email Detection system.

Addresses Decisions:
- D-01: Standard venv and pip with pinned requirements.txt
- D-02: NLTK only for NLP text processing
- D-03: Automated setup and verification utility
- D-04: Modular package structure

Purpose: Set up a clean, modular, and portable Python ML project structure adhering to academic and industry best practices.
Output: Project directory tree, `requirements.txt`, `.gitignore`, `README.md`, and `src/utils/setup_env.py`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-environment-project-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold directory structure, .gitignore, and requirements.txt (D-01, D-04)</name>
  <files>.gitignore, requirements.txt, src/__init__.py, src/data/__init__.py, src/preprocessing/__init__.py, src/features/__init__.py, src/models/__init__.py, src/utils/__init__.py</files>
  <read_first>.planning/phases/01-environment-project-foundation/01-CONTEXT.md</read_first>
  <action>
    Implements decisions D-01 and D-04:
    1. Create directories:
       - `src/data`, `src/preprocessing`, `src/features`, `src/models`, `src/utils`
       - `data/raw`, `data/processed`
       - `models`
       - `notebooks`
       - `app`
    2. Place `__init__.py` files inside `src/` and all its subdirectories (`data`, `preprocessing`, `features`, `models`, `utils`).
    3. Create `.gitignore` ignoring:
       - Virtual environments (`.venv/`, `venv/`, `env/`)
       - Python bytecode (`__pycache__/`, `*.pyc`, `*.pyo`)
       - OS metadata (`.DS_Store`, `Thumbs.db`)
       - Data files (`data/raw/*`, `!data/raw/.gitkeep`, `data/processed/*`, `!data/processed/.gitkeep`)
       - Serialized models (`models/*.joblib`, `models/*.pkl`, `!models/.gitkeep`)
       - Streamlit local state (`.streamlit/`)
    4. Create `.gitkeep` in `data/raw/`, `data/processed/`, `models/`, and `notebooks/` to preserve directory structure in git.
    5. Create `requirements.txt` pinning core libraries for Python 3.10+ on Windows:
       - pandas>=2.0.0
       - numpy>=1.24.0,<2.0.0
       - scikit-learn>=1.3.0
       - xgboost>=2.0.0
       - nltk>=3.8.1
       - streamlit>=1.30.0
       - matplotlib>=3.7.0
       - seaborn>=0.12.0
       - joblib>=1.3.0
  </action>
  <verify>
    Test-Path "requirements.txt", "src/data/__init__.py", "src/utils/__init__.py", "app", "data/raw", "models"
  </verify>
  <acceptance_criteria>
    - requirements.txt exists and contains "scikit-learn", "xgboost", "nltk", "streamlit"
    - .gitignore contains rules for .venv, __pycache__, and models/*.joblib
    - Subpackages under src/ contain __init__.py
  </acceptance_criteria>
  <done>Project directory layout and requirements.txt are configured according to D-01 and D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Implement automated setup & verification utility (D-02, D-03)</name>
  <files>src/utils/setup_env.py</files>
  <read_first>requirements.txt, .planning/phases/01-environment-project-foundation/01-CONTEXT.md</read_first>
  <action>
    Implements decisions D-02 and D-03:
    Write `src/utils/setup_env.py` to provide automated verification and corpus download:
    1. Python version check: ensure `sys.version_info >= (3, 10)`. Warn or fail if running on unsupported Python version.
    2. Dependency verification: attempt importing `pandas`, `numpy`, `sklearn`, `xgboost`, `nltk`, `streamlit`, `matplotlib`, `seaborn`, `joblib`.
       - Report version of each installed library.
       - If any package is missing, report the specific missing package and command to install (`pip install -r requirements.txt`).
    3. NLTK Resource Bootstrapper (D-02, D-03):
       - Automatically download required corpora and tokenizers:
         - `punkt`
         - `punkt_tab`
         - `stopwords`
         - `wordnet`
         - `omw-1.4`
       - Handle SSL context gracefully if default certificates encounter issues on Windows.
       - Use `nltk.download(resource, quiet=True)` with logging.
    4. CLI interface with `argparse`:
       - `--check-only`: only check imports without downloading NLTK corpora.
       - `--download-nltk`: force download of NLTK corpora.
       - Default behavior (no args): verify dependencies and ensure NLTK corpora are downloaded.
    5. Return exit code 0 if all checks pass (or after downloading), or code 1 if required dependencies are missing.
  </action>
  <verify>
    python -c "import ast; ast.parse(open('src/utils/setup_env.py', encoding='utf-8').read())"
  </verify>
  <acceptance_criteria>
    - src/utils/setup_env.py is syntactically valid Python
    - setup_env.py defines check_python_version, check_dependencies, and download_nltk_resources functions
    - setup_env.py includes 'punkt', 'stopwords', and 'wordnet' in NLTK_REQUIREMENTS
  </acceptance_criteria>
  <done>setup_env.py utility is implemented satisfying D-02 and D-03.</done>
</task>

<task type="auto">
  <name>Task 3: Document project setup in README.md</name>
  <files>README.md</files>
  <read_first>.planning/PROJECT.md, requirements.txt, src/utils/setup_env.py</read_first>
  <action>
    Create a comprehensive, college project-ready `README.md`:
    1. Title: "AI-Based Spam Email Detection using Machine Learning"
    2. Overview and Motivation: Explain the goal of classifying email (and SMS) as spam vs ham with explainability and ML model comparisons.
    3. System Architecture & Directory Structure: Show the ASCII tree of the project (`src/`, `data/`, `models/`, `app/`, `notebooks/`).
    4. Prerequisites: Python 3.10+.
    5. Quickstart / Installation Guide:
       - Clone repository
       - Create virtual environment: `python -m venv .venv`
       - Activate environment: Windows PowerShell `.\.venv\Scripts\Activate.ps1` or cmd `.\.venv\Scripts\activate.bat`
       - Install dependencies: `pip install -r requirements.txt`
       - Run environment verification & NLTK setup: `python -m src.utils.setup_env`
    6. Roadmap / Phases status overview:
       - Phase 1: Environment & Project Foundation (Active)
       - Phase 2: Data Ingestion & NLP Preprocessing Pipeline
       - Phase 3: Feature Engineering & Feature Union
       - Phase 4: Model Training Harness & Persistence
       - Phase 5: Evaluation Suite & Explainability Engine
       - Phase 6: Interactive Streamlit Web Application
    7. Tech Stack badges/table (scikit-learn, XGBoost, NLTK, Streamlit, Pandas).
  </action>
  <verify>
    Test-Path "README.md"
  </verify>
  <acceptance_criteria>
    - README.md exists and contains "AI-Based Spam Email Detection using Machine Learning"
    - README.md includes virtualenv creation and setup_env.py instructions
    - README.md outlines directory structure and tech stack
  </acceptance_criteria>
  <done>README.md is created with complete setup documentation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Directory layout matches modular structure (`src/`, `data/`, `models/`, `app/`, `notebooks/`)
- [ ] `requirements.txt` contains all specified libraries
- [ ] `src/utils/setup_env.py` passes syntax validation
- [ ] `README.md` documents setup and workflow
</verification>

<success_criteria>
- All 3 tasks completed
- Verification checks pass
- ENV-01 and ENV-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-project-foundation/01-01-SUMMARY.md`
</output>
