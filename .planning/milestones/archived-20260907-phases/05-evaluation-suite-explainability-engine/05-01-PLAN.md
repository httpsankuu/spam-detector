---
phase: 05-evaluation-suite-explainability-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/evaluation/metrics.py
  - src/evaluation/plots.py
  - src/evaluation/__init__.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
must_haves:
  truths:
    - "Multi-metric evaluation harness calculates Accuracy, Precision, Recall, F1-Score, and ROC-AUC across all trained models on held-out test data."
    - "Detailed classification reports and confusion matrices are generated for every persisted model."
    - "Plotting module generates publication-quality figures: comparative confusion matrix heatmaps and combined multi-model ROC-AUC curves saved to reports/figures/."
  artifacts:
    - path: "src/evaluation/metrics.py"
      provides: "Benchmarking engine and metric summary generator"
      contains: "class ModelEvaluator"
    - path: "src/evaluation/plots.py"
      provides: "Confusion matrix and ROC curve visualization generator"
      contains: "def plot_confusion_matrices"
    - path: "src/evaluation/__init__.py"
      provides: "Exports for evaluation module"
      contains: "from src.evaluation.metrics import"
  key_links:
    - from: "src/evaluation/metrics.py"
      to: "models/*.joblib"
      via: "loads serialized pipelines for test set evaluation"
      pattern: "joblib.load"
    - from: "src/evaluation/plots.py"
      to: "reports/figures/*.png"
      via: "exports matplotlib/seaborn plots"
      pattern: "savefig"
---

<objective>
Implement comprehensive evaluation benchmarks (`EVAL-01`) and visual diagnostic artifacts (`EVAL-02`):
1. `src/evaluation/metrics.py`: Evaluates all 5 trained models on held-out test data (`data/processed/test.csv`) reporting Accuracy, Precision, Recall, F1-Score, and ROC-AUC.
2. `src/evaluation/plots.py`: Generates confusion matrix heatmaps and a combined multi-model ROC-AUC curve saved to `reports/figures/` for viva presentations and the Streamlit app.

Purpose: Provide rigorous, quantitative validation of all spam detection algorithms on unseen test data.
Output: `src/evaluation/metrics.py`, `src/evaluation/plots.py`, and `reports/figures/`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@models/model_metadata.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement multi-metric evaluation harness (EVAL-01)</name>
  <files>src/evaluation/metrics.py</files>
  <read_first>models/model_metadata.json, .planning/REQUIREMENTS.md</read_first>
  <action>
    Create `src/evaluation/metrics.py`:
    1. Define class `ModelEvaluator`:
       - Methods:
         - `load_models(models_dir: str = "models") -> dict[str, Any]`: Loads all `*_pipeline.joblib` models.
         - `evaluate_model(pipeline, X_test, y_test, model_name: str) -> dict`: Computes accuracy, precision, recall, f1, roc_auc, and confusion matrix `[[tn, fp], [fn, tp]]`.
         - `evaluate_all(test_path: str = "data/processed/test.csv") -> pd.DataFrame`: Evaluates all models, prints formatted comparison table, and saves results to `reports/test_benchmark.json`.
    2. Add CLI execution (`python -m src.evaluation.metrics`) running benchmark on `test.csv`.
  </action>
  <verify>
    python -m src.evaluation.metrics
  </verify>
  <acceptance_criteria>
    - src/evaluation/metrics.py contains ModelEvaluator
    - Evaluates all models on held-out test.csv
    - Computes precision, recall, f1, accuracy, roc_auc
    - Outputs reports/test_benchmark.json
  </acceptance_criteria>
  <done>Evaluation benchmark module implemented and executed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement plot generators for confusion matrices and ROC curves (EVAL-02)</name>
  <files>src/evaluation/plots.py</files>
  <read_first>src/evaluation/metrics.py, .planning/REQUIREMENTS.md</read_first>
  <action>
    Create `src/evaluation/plots.py`:
    1. Define functions using `matplotlib` and `seaborn`:
       - `plot_confusion_matrices(results: dict, output_path: str = "reports/figures/confusion_matrices.png")`: Creates a multi-panel subplot grid showing labeled confusion matrices for each model (annotated with Ham/Spam counts and percentages).
       - `plot_roc_curves(evaluator: ModelEvaluator, test_path: str = "data/processed/test.csv", output_path: str = "reports/figures/roc_curves.png")`: Plots ROC curves for all models on a single figure with individual AUC legend scores.
       - `plot_metrics_comparison_bar(results_df: pd.DataFrame, output_path: str = "reports/figures/metrics_comparison.png")`: Generates a grouped bar chart comparing Precision, Recall, and F1 across models.
       - `generate_all_plots()`: Wrapper orchestrating generation and saving of all visual reports.
    2. Add CLI execution (`python -m src.evaluation.plots`) generating and saving all plots to `reports/figures/`.
  </action>
  <verify>
    python -m src.evaluation.plots
  </verify>
  <acceptance_criteria>
    - src/evaluation/plots.py creates reports/figures/ directory if needed
    - Generates reports/figures/confusion_matrices.png
    - Generates reports/figures/roc_curves.png
    - Generates reports/figures/metrics_comparison.png
  </acceptance_criteria>
  <done>Plotting and visual artifact generator implemented and verified.</done>
</task>

<task type="auto">
  <name>Task 3: Export evaluation module in src/evaluation/__init__.py</name>
  <files>src/evaluation/__init__.py</files>
  <read_first>src/evaluation/metrics.py, src/evaluation/plots.py</read_first>
  <action>
    Create `src/evaluation/__init__.py` exporting:
    - `ModelEvaluator`
    - `plot_confusion_matrices`, `plot_roc_curves`, `plot_metrics_comparison_bar`, `generate_all_plots`
  </action>
  <verify>
    python -c "from src.evaluation import ModelEvaluator, generate_all_plots; print('Evaluation exports verified!')"
  </verify>
  <acceptance_criteria>
    - src/evaluation/__init__.py exports evaluation classes and plotting functions
    - Import test passes with exit code 0
  </acceptance_criteria>
  <done>Evaluation exports finalized.</done>
</task>

</tasks>

<verification>
Execute benchmarking and inspect generated figures:
```powershell
python -m src.evaluation.metrics
python -m src.evaluation.plots
Get-ChildItem -Path "reports/figures"
```
</verification>

<success_metrics>
- Quantitative multi-metric benchmark table on test dataset.
- High-resolution visual diagnostic figures saved to `reports/figures/` for presentation.
</success_metrics>
