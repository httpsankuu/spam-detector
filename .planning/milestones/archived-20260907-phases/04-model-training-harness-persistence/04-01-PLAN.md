---
phase: 04-model-training-harness-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/naive_bayes.py
  - src/models/logistic_regression.py
  - src/models/__init__.py
autonomous: true
requirements:
  - MODL-01
  - MODL-02
must_haves:
  truths:
    - "Multinomial Naive Bayes classifier trains as a fast, calibrated baseline with Laplace smoothing (alpha tuning)."
    - "Logistic Regression classifier trains with regularized loss (L2/elasticnet) and calibrated class probabilities."
    - "Both model wrappers encapsulate training, prediction, probability estimation (or calibrated decision scores), and evaluation metrics reporting."
    - "Both models accept UnifiedFeaturePipeline feature matrices and raw text inputs via standalone pipeline wrappers."
  artifacts:
    - path: "src/models/naive_bayes.py"
      provides: "Multinomial Naive Bayes training module and classifier wrapper"
      contains: "class NaiveBayesClassifier"
    - path: "src/models/logistic_regression.py"
      provides: "Logistic Regression training module and classifier wrapper"
      contains: "class LogisticRegressionClassifier"
    - path: "src/models/__init__.py"
      provides: "Exports for model classes"
      contains: "from src.models.naive_bayes import"
  key_links:
    - from: "src/models/naive_bayes.py"
      to: "src/features/feature_pipeline.py"
      via: "trains on composite feature matrix from build_feature_pipeline"
      pattern: "UnifiedFeaturePipeline"
    - from: "src/models/logistic_regression.py"
      to: "src/features/feature_pipeline.py"
      via: "trains on composite feature matrix from build_feature_pipeline"
      pattern: "UnifiedFeaturePipeline"
---

<objective>
Implement and train the baseline linear classifiers:
1. Multinomial Naive Bayes (`MODL-01`) — lightweight probabilistic classifier with Laplace smoothing, ideal for text token counts.
2. Logistic Regression (`MODL-02`) — regularized linear model with well-calibrated class probabilities and transparent log-odds feature coefficients.

Purpose: Provide foundational, explainable linear models for spam detection and establish standard model training interfaces.
Output: `src/models/naive_bayes.py`, `src/models/logistic_regression.py`, and updated `src/models/__init__.py`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/features/feature_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Multinomial Naive Bayes training module (MODL-01)</name>
  <files>src/models/naive_bayes.py</files>
  <read_first>src/features/feature_pipeline.py, .planning/REQUIREMENTS.md</read_first>
  <action>
    Create `src/models/naive_bayes.py`:
    1. Define class `NaiveBayesClassifier`:
       - Constructor parameters: `alpha: float = 1.0` (Laplace smoothing parameter), `fit_prior: bool = True`.
       - Wraps `sklearn.naive_bayes.MultinomialNB`.
       - Methods:
         - `fit(X, y)`
         - `predict(X) -> np.ndarray`
         - `predict_proba(X) -> np.ndarray`
         - `get_feature_log_prob() -> np.ndarray`
         - `evaluate(X, y) -> dict` returning Accuracy, Precision, Recall, F1-score.
       - Include full end-to-end pipeline factory `build_nb_pipeline(...) -> Pipeline` bundling `UnifiedFeaturePipeline` and `MultinomialNB`.
    2. Add CLI execution (`python -m src.models.naive_bayes`) that loads `data/processed/train.csv` and `val.csv`, fits the model, reports metrics, and demonstrates predictions.
  </action>
  <verify>
    python -m src.models.naive_bayes
  </verify>
  <acceptance_criteria>
    - src/models/naive_bayes.py contains NaiveBayesClassifier and build_nb_pipeline
    - Supports fit, predict, predict_proba, and evaluate methods
    - CLI execution trains successfully on train.csv and reports validation metrics
  </acceptance_criteria>
  <done>Multinomial Naive Bayes model module implemented and validated.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Logistic Regression training module (MODL-02)</name>
  <files>src/models/logistic_regression.py</files>
  <read_first>src/features/feature_pipeline.py, src/models/naive_bayes.py</read_first>
  <action>
    Create `src/models/logistic_regression.py`:
    1. Define class `LogisticRegressionClassifier`:
       - Constructor parameters: `C: float = 1.0`, `max_iter: int = 1000`, `class_weight: str | dict | None = "balanced"`, `solver: str = "lbfgs"`, `random_state: int = 42`.
       - Wraps `sklearn.linear_model.LogisticRegression`.
       - Methods:
         - `fit(X, y)`
         - `predict(X) -> np.ndarray`
         - `predict_proba(X) -> np.ndarray`
         - `get_coefficients() -> np.ndarray`
         - `evaluate(X, y) -> dict` returning Accuracy, Precision, Recall, F1-score.
       - Include end-to-end pipeline factory `build_lr_pipeline(...) -> Pipeline` bundling `UnifiedFeaturePipeline` and `LogisticRegression`.
    2. Add CLI execution (`python -m src.models.logistic_regression`) that loads train/val splits, trains, evaluates, and displays top positive (spam) and negative (ham) coefficients.
  </action>
  <verify>
    python -m src.models.logistic_regression
  </verify>
  <acceptance_criteria>
    - src/models/logistic_regression.py contains LogisticRegressionClassifier and build_lr_pipeline
    - Provides access to learned feature coefficients (get_coefficients)
    - CLI execution trains successfully and reports metrics
  </acceptance_criteria>
  <done>Logistic Regression model module implemented and validated.</done>
</task>

<task type="auto">
  <name>Task 3: Export model modules in src/models/__init__.py</name>
  <files>src/models/__init__.py</files>
  <read_first>src/models/naive_bayes.py, src/models/logistic_regression.py</read_first>
  <action>
    Update `src/models/__init__.py` to export:
    - `NaiveBayesClassifier`, `build_nb_pipeline`
    - `LogisticRegressionClassifier`, `build_lr_pipeline`
  </action>
  <verify>
    python -c "from src.models import NaiveBayesClassifier, LogisticRegressionClassifier; print('Model exports verified!')"
  </verify>
  <acceptance_criteria>
    - src/models/__init__.py exports NaiveBayesClassifier and LogisticRegressionClassifier
    - Import test succeeds with code 0
  </acceptance_criteria>
  <done>Baseline model exports updated.</done>
</task>

</tasks>

<verification>
Execute self-checks for both baseline models:
```powershell
python -m src.models.naive_bayes
python -m src.models.logistic_regression
python -c "from src.models import NaiveBayesClassifier, LogisticRegressionClassifier; print('Baseline models ready')"
```
</verification>

<success_metrics>
- Multinomial Naive Bayes trains with Laplace smoothing and outputs probability scores.
- Logistic Regression trains with regularized objective and balanced class weights.
- Both models report precision, recall, and F1 on validation splits.
</success_metrics>
