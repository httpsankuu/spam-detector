---
phase: 06-interactive-streamlit-web-application
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/streamlit_app.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
must_haves:
  truths:
    - "Interactive Streamlit web application allows users to paste raw email/SMS text and select active classifier model."
    - "Displays real-time prediction (Spam vs. Ham badge), confidence probability gauge/bar, and risk severity tier."
    - "Provides explainability breakdown highlighting detected trigger keywords in text, top positive/negative signal contributions, and handcrafted metrics."
    - "Model comparison tab presents comparative evaluation metrics table and renders saved confusion matrices, ROC curves, and bar charts."
  artifacts:
    - path: "app/streamlit_app.py"
      provides: "Interactive Streamlit web demo application"
      contains: "def main"
  key_links:
    - from: "app/streamlit_app.py"
      to: "models/*.joblib"
      via: "loads serialized pipelines for real-time inference"
      pattern: "joblib.load"
    - from: "app/streamlit_app.py"
      to: "src/evaluation/explainability.py"
      via: "uses SpamExplainer for token attribution and keyword highlighting"
      pattern: "SpamExplainer"
    - from: "app/streamlit_app.py"
      to: "reports/figures/*.png"
      via: "displays diagnostic evaluation plots in Model Comparison tab"
      pattern: "st.image"
---

<objective>
Build and verify the complete interactive Streamlit web application (`UI-01`, `UI-02`, `UI-03`, `UI-04`):
1. Single-message live inference interface with model selection, sample presets, prediction badge, confidence bar, and risk tier indicator.
2. Explainability engine integration displaying detected keywords highlighted directly in text, feature contribution bars ($x_i \cdot w_i$), and handcrafted metrics.
3. Model comparison dashboard tab rendering held-out benchmark tables, confusion matrix heatmaps, ROC-AUC curves, and metric comparison charts.

Purpose: Provide a polished, intuitive, college-ready presentation web application demonstrating the end-to-end spam detection system.
Output: `app/streamlit_app.py`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/evaluation/explainability.py
@reports/test_benchmark.json
@reports/figures/confusion_matrices.png
@reports/figures/roc_curves.png
@reports/figures/metrics_comparison.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the complete interactive Streamlit application (UI-01, UI-02, UI-03, UI-04)</name>
  <files>app/streamlit_app.py</files>
  <read_first>src/evaluation/explainability.py, reports/test_benchmark.json, .planning/REQUIREMENTS.md</read_first>
  <action>
    Create `app/streamlit_app.py`:
    1. Streamlit configuration:
       - `st.set_page_config(page_title="AI Spam Detector", page_icon="🛡️", layout="wide")`
       - Modern CSS styling (clean card layouts, alert badges, responsive typography).
    2. Sidebar:
       - Model selector dropdown (`Logistic Regression (Recommended)`, `Linear SVM`, `Multinomial Naive Bayes`, `Random Forest`, `XGBoost`).
       - Dataset & Pipeline info stats.
       - Quick sample loader dropdown ("Phishing Lottery Winner", "Urgent Bank Security Alert", "Work Meeting Sync", "Personal Note") to populate text input instantly for live demo.
    3. Tab 1: Live Spam Classifier (`UI-01`, `UI-02`, `UI-03`)
       - Large text area for email/SMS input.
       - "Analyze Message" action button.
       - Prediction result banner (Green "LEGITIMATE (HAM)" or Red "SPAM DETECTED").
       - Metrics columns: Probability Gauge / Progress Bar, Risk Category (`SAFE`, `LOW RISK`, `MODERATE RISK`, `HIGH RISK`), Active Model name.
       - Keyword highlighting card: highlights detected trigger keywords in red pill badges and renders annotated text.
       - Feature contribution expander: table/barchart of top positive spam signals ($x_i \cdot w_i$) and negative ham signals.
       - Handcrafted statistics card (Caps ratio, link count, exclamation marks, etc.).
    4. Tab 2: Model Benchmarks & Comparison (`UI-04`)
       - Held-out test split benchmark leaderboard table loaded from `reports/test_benchmark.json`.
       - High-resolution visual figures loaded from `reports/figures/`:
         - `confusion_matrices.png`
         - `roc_curves.png`
         - `metrics_comparison.png`
       - Pedagogical explanation explaining trade-offs between linear models and tree ensembles for text classification.
    5. Tab 3: About & Architecture
       - College project summary, team credits, and NLP pipeline diagram.
  </action>
  <verify>
    python -c "import streamlit; import app.streamlit_app; print('Streamlit app code syntax validated successfully!')"
  </verify>
  <acceptance_criteria>
    - app/streamlit_app.py exists and imports cleanly
    - Implements Live Classifier with confidence gauge and explainability
    - Implements Model Comparison tab rendering benchmark table and figure plots
    - Supports preset sample loading and dynamic model selection
  </acceptance_criteria>
  <done>Streamlit web application fully implemented and validated.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end demo verification script</name>
  <files>app/streamlit_app.py</files>
  <read_first>app/streamlit_app.py</read_first>
  <action>
    Test that `app/streamlit_app.py` loads models and produces valid predictions without error when simulated headless:
    Run a python verification script that imports the app helpers, loads models from `models/`, passes sample emails, and confirms non-empty prediction and explanation payloads.
  </action>
  <verify>
    python -c "from src.evaluation.explainability import SpamExplainer; explainer = SpamExplainer(); exp = explainer.explain_text('Claim your free prize now!'); assert exp['prediction'] == 'SPAM'; print('Headless app inference verified successfully!')"
  </verify>
  <acceptance_criteria>
    - Headless verification passes
    - Streamlit app is ready for local execution via `streamlit run app/streamlit_app.py`
  </acceptance_criteria>
  <done>Application verification confirmed.</done>
</task>

</tasks>

<verification>
Verify app syntax and headless execution:
```powershell
python -c "import app.streamlit_app; print('Streamlit app ready')"
```
</verification>

<success_metrics>
- Interactive single-message classification with live confidence gauge.
- Transparent keyword and feature attribution explainability.
- Comparative benchmark dashboard with confusion matrices and ROC curves.
</success_metrics>
