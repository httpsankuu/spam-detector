---
phase: 03-feature-engineering-feature-union
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - src/features/feature_pipeline.py
  - src/features/__init__.py
autonomous: true
requirements:
  - FEAT-03
must_haves:
  truths:
    - "Unified pipeline orchestrates text preprocessing, TF-IDF n-gram vectorization, and scaled handcrafted features into a single composite feature matrix."
    - "Feature scaling (StandardScaler or MinMaxScaler) is applied cleanly to handcrafted numerical features without corrupting sparse TF-IDF text features."
    - "Composite pipeline exposes get_feature_names_out() mapping every column index back to its exact semantic name (n-gram or handcrafted feature)."
    - "Pipeline processes both batch DataFrames and raw single strings seamlessly for training and downstream inference."
  artifacts:
    - path: "src/features/feature_pipeline.py"
      provides: "Unified feature engineering pipeline factory and composite transformer"
      contains: "def build_feature_pipeline"
    - path: "src/features/__init__.py"
      provides: "Exports for unified feature pipeline"
      contains: "from src.features.feature_pipeline import"
  key_links:
    - from: "src/features/feature_pipeline.py"
      to: "src/features/tfidf_features.py"
      via: "uses TFIDFExtractor in text feature branch"
      pattern: "TFIDFExtractor"
    - from: "src/features/feature_pipeline.py"
      to: "src/features/handcrafted_features.py"
      via: "uses HandcraftedFeatureExtractor in numeric branch with scaling"
      pattern: "HandcraftedFeatureExtractor"
---

<objective>
Implement the unified composite feature pipeline (`FEAT-03`) that merges NLP text preprocessing, TF-IDF unigram/bigram representation, and scaled handcrafted statistical spam indicators into a single high-performance feature matrix.

Purpose: Provide a clean, robust, and serializable scikit-learn pipeline step that takes raw email/SMS text inputs and produces an estimator-ready feature matrix with complete feature name tracking for explainability.
Output: `src/features/feature_pipeline.py` and updated `src/features/__init__.py`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/preprocessing/transformer.py
@src/features/tfidf_features.py
@src/features/handcrafted_features.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build composite feature pipeline combining TF-IDF and scaled handcrafted features (FEAT-03)</name>
  <files>src/features/feature_pipeline.py</files>
  <read_first>src/features/tfidf_features.py, src/features/handcrafted_features.py, src/preprocessing/transformer.py</read_first>
  <action>
    Create `src/features/feature_pipeline.py`:
    1. Build `UnifiedFeaturePipeline(BaseEstimator, TransformerMixin)`:
       - Text branch: `TextPreprocessor` -> `TFIDFExtractor(max_features=5000, ngram_range=(1, 2))`
       - Handcrafted branch: `HandcraftedFeatureExtractor` -> `StandardScaler(with_mean=False)` (or `MaxAbsScaler`/`MinMaxScaler` to preserve sparsity and handle non-negative features gracefully, e.g. `StandardScaler(with_mean=False)` or `MaxAbsScaler`)
       - Combines both branches horizontally using `scipy.sparse.hstack` (ensuring memory efficiency and model compatibility).
       - Implements `fit(X, y=None)` and `transform(X)`.
       - Implements `get_feature_names_out() -> np.ndarray`: concatenates TF-IDF vocabulary names with handcrafted feature names in exact column order.
    2. Add factory function `build_feature_pipeline(max_features: int = 5000, ngram_range: tuple = (1, 2), scale_handcrafted: bool = True) -> UnifiedFeaturePipeline`.
    3. Include CLI test verifying end-to-end transformation on sample processed dataset splits (`data/processed/train.csv`).
  </action>
  <verify>
    python -m src.features.feature_pipeline
  </verify>
  <acceptance_criteria>
    - src/features/feature_pipeline.py defines UnifiedFeaturePipeline and build_feature_pipeline
    - Successfully transforms raw text into a combined sparse matrix
    - Column dimensions match tfidf_dim + handcrafted_dim
    - get_feature_names_out() returns exact feature names matching the matrix shape
    - Self-test succeeds on data/processed/train.csv
  </acceptance_criteria>
  <done>Unified feature pipeline is created and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Export pipeline and update src/features/__init__.py</name>
  <files>src/features/__init__.py</files>
  <read_first>src/features/feature_pipeline.py</read_first>
  <action>
    Update `src/features/__init__.py` to export:
    - `UnifiedFeaturePipeline`, `build_feature_pipeline`
    - `TFIDFExtractor`, `build_tfidf_vectorizer`
    - `HandcraftedFeatureExtractor`
  </action>
  <verify>
    python -c "from src.features import UnifiedFeaturePipeline, build_feature_pipeline; print('Feature pipeline exports verified!')"
  </verify>
  <acceptance_criteria>
    - src/features/__init__.py exports UnifiedFeaturePipeline
    - Import test passes with exit code 0
  </acceptance_criteria>
  <done>Feature module exports finalized.</done>
</task>

</tasks>

<verification>
Run end-to-end transformation on the saved training split:
```powershell
python -m src.features.feature_pipeline
python -c "from src.features import build_feature_pipeline; p = build_feature_pipeline(); print(p)"
```
</verification>

<success_metrics>
- Composite pipeline unifies text and handcrafted statistical features without data leakage.
- Handcrafted features are normalized to prevent TF-IDF scale dominance.
- All column names are recoverable for explainability and model inspection in Phase 5.
</success_metrics>
