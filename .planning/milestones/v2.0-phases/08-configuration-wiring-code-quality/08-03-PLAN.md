---
wave: 1
depends_on: []
files_modified:
  - src/utils/setup_env.py
  - src/features/handcrafted.py
  - src/data/email_loader.py
  - src/evaluation/metrics.py
autonomous: true
---

## Phase Goal
Wire all config constants, remove try/except ImportError fallbacks, fix TFIDFExtractor, strip benchmark arrays, fix metadata paths, and clean up miscellaneous code quality issues.

## Objective
Address code quality and portability issues (QUAL-02, QUAL-05) to harden the application and ensure clean coding standards.

## Requirements Addressed
- QUAL-02
- QUAL-05

## Tasks

<task id="08-03-T01" type="implementation">
  <title>Scope SSL Bypass for NLTK Download</title>
  <objective>Restrict the SSL context bypass strictly to the NLTK resource download so the global environment isn't compromised.</objective>
  <read_first>
    - src/utils/setup_env.py
  </read_first>
  <action>
    - In src/utils/setup_env.py, remove the global ssl._create_default_https_context = ssl._create_unverified_context call in configure_ssl_for_nltk().
    - Modify download_nltk_resources() to use a local context manager or temporarily overwrite and restore ssl._create_default_https_context when calling 
ltk.download().
  </action>
  <acceptance_criteria>
    - SSL context bypass is strictly isolated to the NLTK download section.
    - Global ssl._create_default_https_context remains secure post-download.
  </acceptance_criteria>
</task>

<task id="08-03-T02" type="implementation">
  <title>Clean Up Unused Typing Imports</title>
  <objective>Remove unnecessary typing imports from all model implementation modules.</objective>
  <read_first>
    - src/models/naive_bayes.py
    - src/models/logistic_regression.py
    - src/models/linear_svm.py
    - src/models/random_forest.py
    - src/models/xgboost_model.py
  </read_first>
  <action>
    - Remove unused Any, Dict, List, etc. from the typing imports in each model file (e.g., 
aive_bayes.py, logistic_regression.py, linear_svm.py, andom_forest.py, xgboost_model.py).
  </action>
  <acceptance_criteria>
    - No unused 	yping imports remain in the model files.
  </acceptance_criteria>
</task>

<task id="08-03-T03" type="implementation">
  <title>Document digit_ratio computation</title>
  <objective>Add clarifying documentation explaining the logic behind the digit_ratio feature.</objective>
  <read_first>
    - src/features/handcrafted.py
  </read_first>
  <action>
    - In src/features/handcrafted.py, add a comment/docstring to the digit_ratio calculation to explain how it computes the fraction of numeric characters in the text relative to total string length.
  </action>
  <acceptance_criteria>
    - digit_ratio logic is clearly documented with an inline comment.
  </acceptance_criteria>
</task>

<task id="08-03-T04" type="implementation">
  <title>Fix Regex Anchors and os.makedirs Edge Case</title>
  <objective>Fix EMAIL_HEADER_RE anchor and handle empty dirname in os.makedirs safely.</objective>
  <read_first>
    - src/data/email_loader.py
    - src/evaluation/metrics.py
    - src/evaluation/plots.py
  </read_first>
  <action>
    - In src/data/email_loader.py, update EMAIL_HEADER_RE to use e.MULTILINE flag so the ^ anchor behaves correctly for multi-line headers.
    - In src/evaluation/metrics.py (and anywhere else os.makedirs(os.path.dirname(...)) is used), check if os.path.dirname(filepath) is falsy before calling os.makedirs() to avoid throwing an error if the filepath is just a filename.
  </action>
  <acceptance_criteria>
    - EMAIL_HEADER_RE compiles with e.MULTILINE.
    - Calls to os.makedirs(os.path.dirname(X), exist_ok=True) are wrapped in an if os.path.dirname(X): check.
  </acceptance_criteria>
</task>

<verify>
  <automated>python scripts/bootstrap.py --skip-download && pytest tests/test_pipeline.py</automated>
  <fails_when>Unit tests fail or SSL bypass remains globally exposed.</fails_when>
</verify>

<must_haves>
  <truths>
    - Code quality issues are completely mitigated without altering the program's observable execution success.
  </truths>
</must_haves>
