---
phase: 02-data-ingestion-nlp-preprocessing-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/preprocessing/cleaner.py
  - src/preprocessing/tokenizer.py
  - src/preprocessing/transformer.py
autonomous: true
requirements:
  - NLP-01
  - NLP-02
  - NLP-03
must_haves:
  truths:
    - "Text cleaner strips HTML tags, email headers, URLs, non-alphabetic characters, and handles lowercasing cleanly"
    - "Tokenizer & lemmatizer uses NLTK WordNetLemmatizer and stopwords to produce lemmatized token strings"
    - "TextPreprocessor scikit-learn BaseEstimator / TransformerMixin pipeline transformer enables integration into sklearn Pipelines"
  artifacts:
    - path: "src/preprocessing/cleaner.py"
      provides: "Raw text cleaning and regex sanitization"
      contains: "def clean_text"
    - path: "src/preprocessing/tokenizer.py"
      provides: "NLTK tokenization, stopword removal, and lemmatization"
      contains: "def tokenize_and_lemmatize"
    - path: "src/preprocessing/transformer.py"
      provides: "Scikit-learn compatible preprocessing transformer"
      contains: "class TextPreprocessor"
  key_links:
    - from: "src/preprocessing/transformer.py"
      to: "src/preprocessing/cleaner.py"
      via: "calls clean_text in transform method"
      pattern: "clean_text"
    - from: "src/preprocessing/transformer.py"
      to: "src/preprocessing/tokenizer.py"
      via: "calls tokenize_and_lemmatize in transform method"
      pattern: "tokenize_and_lemmatize"
---

<objective>
Build a robust NLP preprocessing pipeline that cleans raw email/SMS text (removing HTML, headers, URLs, punctuation) and applies tokenization, stopword filtering, and WordNet lemmatization wrapped in a reusable scikit-learn transformer.

Addresses Requirements:
- NLP-01: Raw text cleaning (HTML tag removal, email headers/urls/punctuation handling, lowercasing)
- NLP-02: Tokenization, stopword removal, and lemmatization (using NLTK/WordNetLemmatizer)
- NLP-03: Reusable scikit-learn pipeline transformer for training and inference

Purpose: Transform raw, noisy email texts into clean, lemmatized tokens that feed reliably into TF-IDF vectorizers and ML classifiers.
Output: `src/preprocessing/cleaner.py`, `src/preprocessing/tokenizer.py`, and `src/preprocessing/transformer.py`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-ingestion-nlp-preprocessing-pipeline/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement raw text cleaner for emails and SMS (NLP-01)</name>
  <files>src/preprocessing/cleaner.py</files>
  <read_first>src/preprocessing/__init__.py, .planning/REQUIREMENTS.md</read_first>
  <action>
    Create `src/preprocessing/cleaner.py`:
    1. Define `clean_text(text: str, remove_urls: bool = True, remove_html: bool = True, lowercase: bool = True) -> str`:
       - Handle null/non-string input gracefully by returning empty string `""`.
       - Strip HTML markup and tags (`re.compile(r'<.*?>')` or regex equivalent).
       - Strip standard email header prefixes (`Subject:`, `From:`, `To:`, `Cc:`, etc. if present at start).
       - Replace URLs with placeholder `httpaddr` or remove them (`re.compile(r'https?://\S+|www\.\S+')`).
       - Replace email addresses with placeholder `emailaddr` (`re.compile(r'\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Z|a-z]{2,}\b')`).
       - Remove numbers or replace with `number` token (`re.compile(r'\b\d+\b')`).
       - Remove excessive punctuation and special characters, retaining letters and spaces.
       - Collapse repeated whitespace and strip leading/trailing spaces.
    2. Add unit self-check if run directly (`python -m src.preprocessing.cleaner`).
  </action>
  <verify>
    python -c "from src.preprocessing.cleaner import clean_text; print(clean_text('Subject: WINNER! Visit <b>http://spam.com</b> NOW to claim $500!'))"
  </verify>
  <acceptance_criteria>
    - cleaner.py defines clean_text
    - clean_text strips HTML tags like <b>...</b>
    - clean_text replaces or removes URLs and normalizes whitespace
  </acceptance_criteria>
  <done>Raw text cleaner is implemented and verified against HTML/URL spam samples.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tokenizer, stopword removal, and lemmatizer (NLP-02)</name>
  <files>src/preprocessing/tokenizer.py</files>
  <read_first>src/preprocessing/cleaner.py, .planning/REQUIREMENTS.md</read_first>
  <action>
    Create `src/preprocessing/tokenizer.py`:
    1. Import NLTK resources: `nltk.word_tokenize`, `nltk.corpus.stopwords`, `nltk.stem.WordNetLemmatizer`.
    2. Initialize WordNetLemmatizer and English stopwords list with custom exclusion safeguards (retain negations like 'not', 'no').
    3. Define `tokenize_and_lemmatize(text: str, remove_stopwords: bool = True, min_word_length: int = 2) -> List[str]`:
       - Tokenize text into words.
       - Filter out stopwords and tokens shorter than `min_word_length`.
       - Lemmatize tokens (verbs and nouns) using `WordNetLemmatizer().lemmatize(word)`.
       - Return list of lemmatized token strings.
    4. Define `lemmatized_text(text: str) -> str`: convenience function returning tokens joined by single space.
    5. Add CLI execution for quick validation (`python -m src.preprocessing.tokenizer`).
  </action>
  <verify>
    python -c "from src.preprocessing.tokenizer import tokenize_and_lemmatize; print(tokenize_and_lemmatize('Free prize winners are winning tickets'))"
  </verify>
  <acceptance_criteria>
    - tokenizer.py defines tokenize_and_lemmatize and lemmatized_text
    - Words like 'winners', 'winning' are lemmatized to 'winner', 'win'
    - Stopwords are excluded by default
  </acceptance_criteria>
  <done>Tokenizer, stopword remover, and lemmatizer module is implemented and verified.</done>
</task>

<task type="auto">
  <name>Task 3: Build reusable scikit-learn Pipeline Transformer (NLP-03)</name>
  <files>src/preprocessing/transformer.py</files>
  <read_first>src/preprocessing/cleaner.py, src/preprocessing/tokenizer.py, .planning/REQUIREMENTS.md</read_first>
  <action>
    Create `src/preprocessing/transformer.py`:
    1. Define class `TextPreprocessor(BaseEstimator, TransformerMixin)`:
       - Constructor arguments: `remove_urls: bool = True`, `remove_html: bool = True`, `remove_stopwords: bool = True`, `lemmatize: bool = True`.
       - `fit(X, y=None)`: returns `self`.
       - `transform(X) -> List[str]` or `np.ndarray`:
         - Handles `pd.Series`, list of strings, or 1D `np.ndarray`.
         - Applies `clean_text` followed by `lemmatized_text` to each document.
         - Returns clean, space-joined string documents ready for vectorization.
    2. Demonstrate integration with scikit-learn `Pipeline` (e.g. `Pipeline([('prep', TextPreprocessor()), ('tfidf', TfidfVectorizer())])`).
    3. Add CLI verification (`python -m src.preprocessing.transformer`).
  </action>
  <verify>
    python -m src.preprocessing.transformer
  </verify>
  <acceptance_criteria>
    - transformer.py defines TextPreprocessor inheriting from BaseEstimator and TransformerMixin
    - TextPreprocessor transforms raw text Series into clean lemmatized text
    - TextPreprocessor can be embedded in a scikit-learn Pipeline
  </acceptance_criteria>
  <done>TextPreprocessor pipeline transformer is implemented and verified with scikit-learn.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `clean_text` removes HTML, URLs, headers, and punctuation
- [ ] `tokenize_and_lemmatize` removes stopwords and lemmatizes tokens
- [ ] `TextPreprocessor` operates seamlessly in a scikit-learn Pipeline
</verification>

<success_criteria>
- All 3 tasks completed
- NLP-01, NLP-02, and NLP-03 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-ingestion-nlp-preprocessing-pipeline/02-02-SUMMARY.md`
</output>
