---
phase: 02-data-ingestion-nlp-preprocessing-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/email_loader.py
  - src/data/sms_loader.py
  - src/data/split_data.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
must_haves:
  truths:
    - "Email dataset loader can ingest email spam datasets (e.g. CSV/raw text formats) and produce a normalized DataFrame with 'text' and 'label' (1=spam, 0=ham) columns"
    - "SMS dataset loader ingests UCI SMS Spam Collection tab-separated data into identical schema ('text', 'label')"
    - "Data splitter creates stratified train, validation, and test splits with fixed random seed, preserving class ratios"
    - "A sample starter email corpus is bundled or auto-fetched so the pipeline is immediately runnable offline without requiring external API keys"
  artifacts:
    - path: "src/data/email_loader.py"
      provides: "Email dataset loader and schema normalization"
      contains: "class EmailDataLoader"
    - path: "src/data/sms_loader.py"
      provides: "UCI SMS Spam Collection ingestion module"
      contains: "class SMSDataLoader"
    - path: "src/data/split_data.py"
      provides: "Stratified dataset partitioning utility"
      contains: "def split_dataset"
  key_links:
    - from: "src/data/split_data.py"
      to: "src/data/email_loader.py"
      via: "split_dataset accepts normalized DataFrame from email loader"
      pattern: "split_dataset"
---

<objective>
Implement dataset ingestion and stratified partitioning modules for both email and SMS spam datasets, enforcing a standardized schema (`text`, `label` where 1=spam, 0=ham).

Addresses Requirements:
- DATA-01: Email dataset loader (Enron / Kaggle / SpamAssassin)
- DATA-02: Standardized data schema with stratified train/test split
- DATA-03: SMS Spam Collection (UCI) dataset ingestion module

Purpose: Ensure consistent, high-quality, reproducible data input across all downstream NLP preprocessing, feature engineering, and model training phases.
Output: `src/data/email_loader.py`, `src/data/sms_loader.py`, and `src/data/split_data.py`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement email dataset loader with schema normalization (DATA-01, DATA-02)</name>
  <files>src/data/email_loader.py</files>
  <read_first>src/data/__init__.py, .planning/REQUIREMENTS.md</read_first>
  <action>
    Create `src/data/email_loader.py`:
    1. Define class `EmailDataLoader`:
       - Method `load_from_csv(filepath: str, text_col: str, label_col: str, spam_value: Any = 1) -> pd.DataFrame`
       - Method `load_raw_directory(spam_dir: str, ham_dir: str) -> pd.DataFrame`
       - Method `get_sample_corpus() -> pd.DataFrame`: returns an embedded balanced starter dataset of 30+ realistic spam and ham emails for offline immediate testing and unit tests.
    2. Normalize output DataFrame:
       - Columns strictly: `['text', 'label']`
       - `text`: string, non-null, stripped
       - `label`: int (1 for spam, 0 for ham)
    3. Drop NaN/null texts and report summary counts (total, ham, spam, spam_ratio).
    4. Provide CLI execution (`python -m src.data.email_loader --sample`) to verify and export sample data to `data/raw/sample_emails.csv`.
  </action>
  <verify>
    python -m src.data.email_loader --sample
  </verify>
  <acceptance_criteria>
    - src/data/email_loader.py contains EmailDataLoader class
    - Running python -m src.data.email_loader --sample outputs valid summary and creates data/raw/sample_emails.csv
    - DataFrame columns contain strictly 'text' and 'label'
  </acceptance_criteria>
  <done>Email loader is implemented and produces validated, standardized DataFrames.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SMS Spam Collection dataset loader (DATA-03)</name>
  <files>src/data/sms_loader.py</files>
  <read_first>src/data/email_loader.py, .planning/REQUIREMENTS.md</read_first>
  <action>
    Create `src/data/sms_loader.py`:
    1. Define class `SMSDataLoader`:
       - Method `load_uci_file(filepath: str) -> pd.DataFrame`: reads the classic UCI tab-separated format (columns: `label_raw\ttext`), mapping 'spam' -> 1 and 'ham' -> 0.
       - Method `download_uci_dataset(dest_path: str = "data/raw/SMSSpamCollection") -> str`: attempts downloading from the official UCI / GitHub mirror archive if not present locally.
       - Method `get_sample_sms() -> pd.DataFrame`: returns starter SMS messages (spam & ham) for testing when offline.
    2. Normalize output DataFrame to `['text', 'label']` with integer labels.
    3. Provide CLI entrypoint (`python -m src.data.sms_loader --sample`).
  </action>
  <verify>
    python -m src.data.sms_loader --sample
  </verify>
  <acceptance_criteria>
    - src/data/sms_loader.py contains SMSDataLoader class
    - Running python -m src.data.sms_loader --sample exits 0 and prints sample SMS counts
    - UCI loader maps 'spam' to 1 and 'ham' to 0
  </acceptance_criteria>
  <done>SMS dataset loader is implemented with UCI format support and schema normalization.</done>
</task>

<task type="auto">
  <name>Task 3: Implement stratified train/val/test data splitter (DATA-02)</name>
  <files>src/data/split_data.py</files>
  <read_first>src/data/email_loader.py, .planning/REQUIREMENTS.md</read_first>
  <action>
    Create `src/data/split_data.py`:
    1. Define `split_dataset(df: pd.DataFrame, test_size: float = 0.2, val_size: float = 0.1, random_state: int = 42) -> Tuple[pd.DataFrame, pd.DataFrame, pd.DataFrame]`:
       - Uses `sklearn.model_selection.train_test_split` with `stratify=df['label']`.
       - Validates input contains `['text', 'label']` and at least 2 classes.
       - Computes split proportions accurately: train (e.g. 70%), val (10%), test (20%).
    2. Define `save_splits(train_df: pd.DataFrame, val_df: pd.DataFrame, test_df: pd.DataFrame, output_dir: str = "data/processed") -> Dict[str, str]`:
       - Saves `train.csv`, `val.csv`, `test.csv` into `output_dir`.
       - Verifies row count sum equals original and class distribution is consistent across splits.
    3. Add CLI support (`python -m src.data.split_data --input data/raw/sample_emails.csv --output-dir data/processed`).
  </action>
  <verify>
    python -m src.data.split_data --input data/raw/sample_emails.csv --output-dir data/processed
  </verify>
  <acceptance_criteria>
    - src/data/split_data.py defines split_dataset and save_splits
    - Running split_data CLI on sample_emails.csv generates train.csv, val.csv, and test.csv in data/processed
    - Splits preserve class stratification
  </acceptance_criteria>
  <done>Stratified dataset splitting and persistence module is verified and working.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Email and SMS loaders load data into standard `['text', 'label']` schema
- [ ] Stratified splitter produces `train.csv`, `val.csv`, and `test.csv` in `data/processed/`
- [ ] All data loading and splitting CLIs run without error
</verification>

<success_criteria>
- All 3 tasks completed
- DATA-01, DATA-02, and DATA-03 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-ingestion-nlp-preprocessing-pipeline/02-01-SUMMARY.md`
</output>
