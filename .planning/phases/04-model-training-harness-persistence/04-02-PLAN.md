---
phase: 04-model-training-harness-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - src/models/svm_classifier.py
  - src/models/tree_classifier.py
  - src/models/trainer.py
  - src/models/__init__.py
autonomous: true
requirements:
  - MODL-03
  - MODL-04
  - MODL-05
must_haves:
  truths:
    - "Linear SVM classifier (LinearSVC / SGDClassifier with hinge loss) trains with maximum margin separation on high-dimensional text features."
    - "Tree ensemble classifier (Random Forest and XGBoost) trains non-linear feature interactions with calibrated predictions."
    - "Unified model training and persistence harness trains, evaluates, compares, and serializes all models with metadata to models/*.joblib."
    - "Serialized pipeline bundles feature engineering and model estimator into a single deployable artifact ready for Streamlit web app."
  artifacts:
    - path: "src/models/svm_classifier.py"
      provides: "Linear Support Vector Machine classifier wrapper"
      contains: "class SVMClassifier"
    - path: "src/models/tree_classifier.py"
      provides: "Random Forest and XGBoost ensemble classifier wrapper"
      contains: "class EnsembleClassifier"
    - path: "src/models/trainer.py"
      provides: "Multi-model training orchestrator and joblib serialization pipeline"
      contains: "def train_and_persist_all"
    - path: "src/models/__init__.py"
      provides: "Complete exports for models package"
      contains: "from src.models.trainer import"
  key_links:
    - from: "src/models/trainer.py"
      to: "src/features/feature_pipeline.py"
      via: "bundles UnifiedFeaturePipeline with estimators"
      pattern: "build_feature_pipeline"
    - from: "src/models/trainer.py"
      to: "models/*.joblib"
      via: "serializes trained model artifacts with joblib"
      pattern: "joblib.dump"
---

<objective>
Implement Linear SVM and tree ensemble (Random Forest / XGBoost) classifiers (`MODL-03`, `MODL-04`), and build a comprehensive training and serialization harness (`MODL-05`) that trains all models, evaluates their performance, and serializes deployable pipelines to `models/*.joblib`.

Purpose: Complete the model selection suite with high-margin linear and non-linear tree ensemble algorithms, and establish the persistence mechanism that feeds the Streamlit web demo and evaluation suite.
Output: `src/models/svm_classifier.py`, `src/models/tree_classifier.py`, `src/models/trainer.py`, and updated `src/models/__init__.py`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/features/feature_pipeline.py
@src/models/naive_bayes.py
@src/models/logistic_regression.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Linear Support Vector Machine classifier (MODL-03)</name>
  <files>src/models/svm_classifier.py</files>
  <read_first>src/models/logistic_regression.py, .planning/REQUIREMENTS.md</read_first>
  <action>
    Create `src/models/svm_classifier.py`:
    1. Define class `SVMClassifier`:
       - Constructor parameters: `C: float = 1.0`, `loss: str = "squared_hinge"`, `class_weight: str | dict | None = "balanced"`, `max_iter: int = 2000`, `random_state: int = 42`.
       - Wraps `sklearn.svm.LinearSVC` (or `CalibratedClassifierCV(LinearSVC)` to provide probability estimates required for confidence scores).
       - Methods: `fit(X, y)`, `predict(X)`, `predict_proba(X)`, `decision_function(X)`, `evaluate(X, y)`.
       - Provide factory `build_svm_pipeline(...) -> Pipeline`.
    2. Add CLI execution (`python -m src.models.svm_classifier`) testing training and metrics reporting.
  </action>
  <verify>
    python -m src.models.svm_classifier
  </verify>
  <acceptance_criteria>
    - src/models/svm_classifier.py contains SVMClassifier and build_svm_pipeline
    - Supports probability calibration via CalibratedClassifierCV
    - CLI execution trains and outputs validation metrics
  </acceptance_criteria>
  <done>Linear SVM classifier implemented and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Tree Ensemble classifier - Random Forest & XGBoost (MODL-04)</name>
  <files>src/models/tree_classifier.py</files>
  <read_first>src/models/svm_classifier.py, .planning/REQUIREMENTS.md</read_first>
  <action>
    Create `src/models/tree_classifier.py`:
    1. Define class `EnsembleClassifier`:
       - Constructor parameters: `model_type: str = "random_forest"` (options: `"random_forest"`, `"xgboost"`), `n_estimators: int = 100`, `max_depth: int | None = 15`, `random_state: int = 42`.
       - For `"random_forest"`: wraps `sklearn.ensemble.RandomForestClassifier`.
       - For `"xgboost"`: wraps `xgboost.XGBClassifier(eval_metric="logloss", use_label_encoder=False)`.
       - Methods: `fit(X, y)`, `predict(X)`, `predict_proba(X)`, `get_feature_importances()`, `evaluate(X, y)`.
       - Provide factory `build_ensemble_pipeline(model_type: str = "random_forest", ...) -> Pipeline`.
    2. Add CLI execution (`python -m src.models.tree_classifier`) testing both Random Forest and XGBoost on train/val data.
  </action>
  <verify>
    python -m src.models.tree_classifier
  </verify>
  <acceptance_criteria>
    - src/models/tree_classifier.py contains EnsembleClassifier supporting both Random Forest and XGBoost
    - Provides feature importances for non-linear feature analysis
    - CLI execution succeeds for both model types
  </acceptance_criteria>
  <done>Tree ensemble classifier module implemented and verified.</done>
</task>

<task type="auto">
  <name>Task 3: Implement multi-model training orchestrator & model persistence (MODL-05)</name>
  <files>src/models/trainer.py, src/models/__init__.py</files>
  <read_first>src/models/naive_bayes.py, src/models/logistic_regression.py, src/models/svm_classifier.py, src/models/tree_classifier.py</read_first>
  <action>
    Create `src/models/trainer.py`:
    1. Define `train_and_persist_all(train_path: str = "data/processed/train.csv", val_path: str = "data/processed/val.csv", output_dir: str = "models") -> dict`:
       - Trains 5 model pipelines end-to-end bundling `UnifiedFeaturePipeline` and each classifier:
         1. `naive_bayes`: Multinomial Naive Bayes
         2. `logistic_regression`: Balanced Logistic Regression
         3. `linear_svm`: Calibrated Linear Support Vector Machine
         4. `random_forest`: Random Forest Classifier
         5. `xgboost`: XGBoost Classifier
       - Evaluates each model on the validation split (`val.csv`) across Accuracy, Precision, Recall, F1-Score, and ROC-AUC.
       - Serializes each complete pipeline to `models/{model_name}_pipeline.joblib`.
       - Saves training metadata and benchmark leaderboard to `models/model_metadata.json`.
    2. Add CLI execution (`python -m src.models.trainer`) that executes full training harness and prints leaderboard.
    3. Update `src/models/__init__.py` to export all classes and `train_and_persist_all`.
  </action>
  <verify>
    python -m src.models.trainer
  </verify>
  <acceptance_criteria>
    - src/models/trainer.py defines train_and_persist_all
    - Serializes 5 pipeline files (*.joblib) and model_metadata.json into models/
    - Prints comparison leaderboard of all trained models
    - Running python -m src.models.trainer exits with code 0
  </acceptance_criteria>
  <done>Multi-model training and persistence pipeline implemented and executed.</done>
</task>

</tasks>

<verification>
Execute the full multi-model training harness and inspect serialized models:
```powershell
python -m src.models.trainer
Get-ChildItem -Path "models"
python -c "import joblib; model = joblib.load('models/logistic_regression_pipeline.joblib'); print(model.predict(['Claim your $1000 cash prize!']))"
```
</verification>

<success_metrics>
- Linear SVM and tree ensembles (Random Forest + XGBoost) trained.
- Complete pipelines (preprocessing + feature engineering + model) serialized to `models/*.joblib`.
- Single string inference works seamlessly through the persisted joblib artifact.
</success_metrics>
